language: generic
stages:
  - compile
  
# blocklist  
branches:
  except:
  - master

# safelist
branches:
  only:
  - working
  - 
jobs:
  include:
    - stage: compile
      script: www.google.com
      script: curl -k -X POST "https://jaas.wdf.sap.corp:31078/job/sap-newcode/build?token=Mdali"
      
