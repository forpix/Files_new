language: generic 
stages:
  - compile
  - test
  - deploy
  - new stage
env:
  - FOO=one
  - FOO=two
  
# blocklist  
branches:
  except:
  - master

# safelist
branches:
  only:
  - working
  - 
jobs:
  include:
    - stage: Compile
      script: pwd
    - stage: Test
      script: pwd
    - stage: Package
      script: echo$DEPLOY DEPLOY
