language: generic
stages:
  - compile
  
addons:
    apt:
      update: true
  
# blocklist  
branches:
  except:
  - master

# safelist
branches:
  only:
  - working
  - 
jobs:
  include:
    - stage: compile
      script: sudo apt-get install autotools-dev;sudo apt-get install automake
      script: firefox "https://jaas.wdf.sap.corp:31078/job/sap-newcode/build?token=Mdali"
